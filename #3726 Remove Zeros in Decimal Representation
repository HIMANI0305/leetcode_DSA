class Solution {
    public long removeZeros(long n) {
        String s = Long.toString(n);           
        String noZeros = s.replace("0", "");   
        return Long.parseLong(noZeros);        
    }
}
